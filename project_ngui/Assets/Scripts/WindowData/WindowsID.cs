using UnityEngine;
using System.Collections;

//窗口类型（与配表中ID一致）
public enum eWindowsID {
    GameStartPanel = 1,
    LoginPanel = 2,
    LoadingUI = 3,
    CreateRoleUI = 4,
    BattleUI = 5,
    MainUI = 6,
    StageUI = 7,
    SelecteRole = 8,
    OfflineBattle=9,
}
